class Topology {
    constructor(musicPath, length){
        this.path = musicPath;
        this.topology = {
            height =[],
            width = 50,
            length = 50,
        }
    }
}